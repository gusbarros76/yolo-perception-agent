# Architecture

Perception-first flow:

Input -> Perception -> Structured Output -> Consumption by agents.

Perception is explicitly separated from decision-making; the system emits structured observations while downstream agents interpret and act on them.
